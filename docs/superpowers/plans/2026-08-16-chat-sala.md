# Chat da Sala de Partida — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Chat de conversa geral nas salas de partida, trafegando pelo WebSocket próprio, com mensagens autoexcluídas após 5 minutos, widget no canto inferior direito, e custo de performance ≈ zero.

**Architecture:** As mensagens vão pelo WebSocket existente (`api/src/realtime/index.ts`), que já tem os participantes assinados. Envio = validação no servidor (ban, Riot vinculado, rate limit) → `INSERT` em `sala_mensagens` → fan-out em memória. Histórico via `GET /api/matches/:id/mensagens` no load inicial e no reconnect. Purge de mensagens > 5 min no cron existente.

**Tech Stack:** Drizzle ORM + migrations SQL versionadas; serviço WebSocket `ws` (api); Express (api); React 19 + Vite + motion (web).

**Spec:** `docs/superpowers/specs/2026-08-16-chat-sala-design.md` · **ADR:** ADR-040

## Global Constraints

- Nenhuma regra de negócio no cliente — validação de chat (tamanho, ban, Riot, rate limit) é 100% no servidor.
- O socket continua sem transportar dados de sala: `match_update` só avisa o id (não mudar). Chat é caso novo à parte (`chat_message`).
- Partidas finalizadas não têm chat: widget só em `preenchendo`, `confirmacao`, `iniciando_partida`, `partida_iniciada`, `aguardando_revisao`.
- Permissão: participante de `match_players` (qualquer vaga/cargo) OU staff (`admin`/`moderador`/`proprietario`), com `users.riot_id` preenchido.
- Mensagens: `body` 1–200 chars, autoexcluídas após 5 min (UI e banco).
- Nenhum arquivo novo passa de ~400 linhas.
- Sem `dangerouslySetInnerHTML` no chat (texto puro).
- Verificação obrigatória antes de marcar `done`: comandos executados e saída real colada no campo `evidence`.

## Contratos (valem para todas as tasks)

**Wire type** (`ApiSalaChatMensagem`, shape do servidor para o cliente):

```ts
interface ApiSalaChatMensagem {
  id: number;              // bigserial
  user_id: string;         // uuid do remetente
  nome: string;            // users.display_name
  avatar: string | null;   // users.avatar_url
  body: string;            // 1..200 chars
  created_at: string;      // ISO
}
```

**WS cliente → servidor:** `{ type: "chat_send", matchId: number|string, body: string }`

**WS servidor → cliente:** `{ type: "chat_message", matchId: number, msg: ApiSalaChatMensagem }` · erro: `{ type: "chat_error", error: "<codigo>" }`

**Códigos de erro de chat:** `nao_assinado`, `nao_autenticado`, `body_invalido`, `conta_banida`, `riot_id_necessario`, `rate_limited`, `erro_interno`.

**HTTP:** `GET /api/matches/:id/mensagens?after=<id>` → `ApiSalaChatMensagem[]` (ASC, máx 200). Auth: sessão + participante/staff. Sem permissão → 403 `{ error: "sem_permissao" }`.

**IDs de componente (status-server):** migration = `db.chat.mensagens` (criar via `add_component`), API = `app.chat.mensagens` (criar via `add_component`).

---

### Task 1: Schema `sala_mensagens` + migration

**Files:**
- Modify: `db/schema/matches.ts`
- Generate: `db/migrations/0018_*.sql` (nome aleatório do drizzle-kit)
- Verify: `npx drizzle-kit generate`

**Interfaces:**
- Produces: `salaMensagens` exportado de `db/schema/index.ts` (via `export * from "./matches.js"` já existente). Colunas: `id: bigserial`, `matchId: uuid`, `userId: uuid`, `body: text`, `createdAt: timestamp`. Índices `sala_mensagens_match_id_id_idx (matchId, id)` e `sala_mensagens_created_at_idx (createdAt)`.

- [ ] **Step 1: Adicionar a tabela ao schema**

No topo de `db/schema/matches.ts` (linha 1-15), trocar o import de `"drizzle-orm/pg-core"` para incluir `bigserial` e `check`, e importar `sql` de `"drizzle-orm"`:

```ts
import {
  pgTable,
  uuid,
  varchar,
  text,
  boolean,
  integer,
  timestamp,
  jsonb,
  numeric,
  bigserial,
  primaryKey,
  index,
  check,
} from "drizzle-orm/pg-core";
import { sql } from "drizzle-orm";
import { users } from "./identidade.js";
import { games } from "./games.js";
```

Ao final do arquivo (após `matchCodes`, linha 120), adicionar:

```ts
// Chat da sala (ADR-040): conversa geral entre participantes. Mensagens são
// autoexcluídas após 5 min (cron + purge preguiçoso) — a tabela nunca acumula.
export const salaMensagens = pgTable(
  "sala_mensagens",
  {
    id: bigserial("id", { mode: "number" }).primaryKey(),
    matchId: uuid("match_id")
      .notNull()
      .references(() => matches.id, { onDelete: "cascade" }),
    userId: uuid("user_id")
      .notNull()
      .references(() => users.id),
    body: text("body").notNull(),
    createdAt: timestamp("created_at", { mode: "date" }).defaultNow().notNull(),
  },
  (table) => [
    index("sala_mensagens_match_id_id_idx").on(table.matchId, table.id),
    index("sala_mensagens_created_at_idx").on(table.createdAt),
    check("sala_mensagens_body_len", sql`char_length(${table.body}) BETWEEN 1 AND 200`),
  ]
);
```

- [ ] **Step 2: Gerar a migration**

Run (na raiz): `npx drizzle-kit generate`
Expected: cria `db/migrations/0018_<random>_<name>.sql` com `CREATE TABLE IF NOT EXISTS "sala_mensagens"`, os 2 índices e o CHECK.

- [ ] **Step 3: Conferir o SQL gerado**

Read o arquivo `db/migrations/0018_*.sql` e confirme: tabela `sala_mensagens`, FKs para `matches(id)` (ON DELETE CASCADE) e `users(id)`, índice `(match_id, id)`, índice `(created_at)`, CHECK `char_length(body) BETWEEN 1 AND 200`. Se o drizzle-kit não emitiu o CHECK, adicionar manualmente ao arquivo:

```sql
ALTER TABLE "sala_mensagens" ADD CONSTRAINT "sala_mensagens_body_len" CHECK (char_length(body) BETWEEN 1 AND 200);
```

- [ ] **Step 4: Typecheck do schema**

Run (raiz): `npx tsc --noEmit -p db/tsconfig.json` — se não existir `db/tsconfig.json`, rodar em `api/`: `npm run build`
Expected: exit 0, sem erros de tipo.

- [ ] **Step 5: Commit**

```bash
git add db/schema/matches.ts db/migrations/0018_*.sql
git commit -m "feat(db): tabela sala_mensagens para chat de sala (ADR-040)"
```

---

### Task 2: API — `GET /api/matches/:id/mensagens` (histórico + purge preguiçoso) + SDK

**Files:**
- Modify: `api/src/routes/matches.ts` (imports linha 3-9; nova rota após `GET /:id`, linha 142)
- Modify: `web/src/lib/api.ts` (tipo + SDK `api.matches.mensagens`)

**Interfaces:**
- Consumes: `salaMensagens` (Task 1), `getAuthUser` (já existe em `api/src/lib/match-flow.ts:30`), `userRoles` (já importado na linha 5 de matches.ts).
- Produces: `GET /api/matches/:id/mensagens?after=<id>` → `ApiSalaChatMensagem[]`; `api.matches.mensagens(id, after?)` no SDK.

- [ ] **Step 1: Atualizar imports de `matches.ts`**

Linha 3 (`"drizzle-orm"`): adicionar `lt`, `asc`:

```ts
import { eq, and, gt, lt, asc, inArray, desc } from "drizzle-orm";
```

Linha 6: incluir `salaMensagens`:

```ts
import { matches, matchPlayers, matchResults, matchCodes, salaMensagens } from "../../../db/schema/matches.js";
```

- [ ] **Step 2: Adicionar a rota de histórico**

Inserir após a rota `GET /:id` (que termina na linha 142, antes de `POST /` na linha 145):

```ts
// GET /api/matches/:id/mensagens - Histórico do chat (ADR-040). Só
// participante/staff. Faz purge preguiçoso das mensagens expiradas (>5min).
const CINCO_MINUTOS_MS = 5 * 60 * 1000;
const ROLES_STAFF_CHAT = ["admin", "moderador", "proprietario"];

matchesRouter.get("/:id/mensagens", async (req, res) => {
  try {
    const match = await resolverSala(req.params.id);
    if (!match) return res.status(404).json({ error: "Partida não encontrada" });

    const user = await getAuthUser(req);
    if (!user) return res.status(401).json({ error: "nao_autenticado" });

    const [participante] = await db
      .select({ userId: matchPlayers.userId })
      .from(matchPlayers)
      .where(and(eq(matchPlayers.matchId, match.id), eq(matchPlayers.userId, user.id)))
      .limit(1);
    const [staff] = await db
      .select({ role: userRoles.role })
      .from(userRoles)
      .where(and(eq(userRoles.userId, user.id), inArray(userRoles.role, ROLES_STAFF_CHAT)))
      .limit(1);
    if (!participante && !staff) return res.status(403).json({ error: "sem_permissao" });

    // Purge preguiçoso: apaga o que já expirou antes de ler (tabela pequena).
    await db.delete(salaMensagens).where(lt(salaMensagens.createdAt, new Date(Date.now() - CINCO_MINUTOS_MS)));

    const after = Number(req.query.after);
    const condicoes = [eq(salaMensagens.matchId, match.id)];
    if (Number.isFinite(after) && after > 0) condicoes.push(gt(salaMensagens.id, after));
    const rows = await db
      .select()
      .from(salaMensagens)
      .where(and(...condicoes))
      .orderBy(asc(salaMensagens.id))
      .limit(200);

    const userIds = [...new Set(rows.map((r) => r.userId))];
    const usersRows: any[] = userIds.length ? await db.select().from(users).where(inArray(users.id, userIds)) : [];
    const userMap = new Map<string, any>(usersRows.map((u: any) => [u.id, u] as [string, any]));

    const msgs = rows.map((r) => {
      const u = userMap.get(r.userId);
      return {
        id: r.id,
        user_id: r.userId,
        nome: u?.displayName || "Jogador",
        avatar: u?.avatarUrl ?? null,
        body: r.body,
        created_at: r.createdAt instanceof Date ? r.createdAt.toISOString() : new Date(r.createdAt).toISOString(),
      };
    });
    return res.json(msgs);
  } catch (error: any) {
    return res.status(500).json({ error: error?.message || "Erro ao buscar mensagens" });
  }
});
```

- [ ] **Step 3: Typecheck da API**

Run (em `api/`): `npm run build`
Expected: exit 0 (tsc compila sem erro).

- [ ] **Step 4: SDK no front — tipo**

Em `web/src/lib/api.ts`, logo após a interface `ApiSalaResultado` (linha ~298), adicionar:

```ts
/** Mensagem do chat de sala (ADR-040). */
export interface ApiSalaChatMensagem {
  id: number;
  user_id: string;
  nome: string;
  avatar: string | null;
  body: string;
  created_at: string;
}
```

- [ ] **Step 5: SDK no front — método**

Em `web/src/lib/api.ts`, na interface `ApiMatchesSdk` (linha 376), adicionar após `verificar`:

```ts
  /** Histórico do chat da sala (ADR-040) — a partir do id `after` (bigserial). */
  mensagens: (id: number | string, after?: number) =>
    api.get<ApiSalaChatMensagem[]>(`/matches/${id}/mensagens${after && after > 0 ? `?after=${after}` : ""}`),
```

E no objeto `matches:` (após o membro `verificar`, linha ~759):

```ts
    mensagens: (id: number | string, after?: number) =>
      api.get<ApiSalaChatMensagem[]>(`/matches/${id}/mensagens${after && after > 0 ? `?after=${after}` : ""}`),
```

- [ ] **Step 6: Typecheck do front**

Run (em `web/`): `npm run lint`
Expected: exit 0 (tsc --noEmit sem erro).

- [ ] **Step 7: Commit**

```bash
git add api/src/routes/matches.ts web/src/lib/api.ts
git commit -m "feat(api): histórico do chat de sala GET /api/matches/:id/mensagens + SDK"
```

---

### Task 3: Realtime — `chat_send` + fan-out + rate limit

**Files:**
- Modify: `api/src/realtime/index.ts` (constantes após linha 44; maps após linha 67; `tratarMensagem` linhas 154-194; `removerDaSala` linhas 125-134; `ws.on("close")` linha 290)

**Interfaces:**
- Consumes: nada das tasks anteriores (usa `authPool` e `rooms`/`socketRoom` existentes). 
- Produces: tratador de `chat_send`; fan-out `chat_message` para o `rooms` map da sala; erros `chat_error`.

- [ ] **Step 1: Constantes e estado de rate limit + uuid da sala**

Após a linha 44 (`uuidRegex`), adicionar:

```ts
// ── Chat (ADR-040): validações e rate limit do chat_send. ──
const CHAT_MIN_INTERVALO_MS = 1_000;        // 1 msg/s por usuário
const CHAT_MAX_EM_5MIN = 40;                // teto por janela
const CHAT_JANELA_MS = 5 * 60 * 1000;       // janela do rate limit
const CHAT_BODY_MAX = 200;
```

Após o map `socketUser` (linha 63), adicionar:

```ts
// Socket → uuid interno do match (para o INSERT do chat, que referencia matches.id).
const socketMatchId = new Map<WebSocket, string>();
// userId → timestamps das mensagens (rate limit em memória; processo único).
const chatTimestamps = new Map<string, number[]>();
```

- [ ] **Step 2: Limpar `socketMatchId`**

Em `removerDaSala` (linha 125), adicionar a primeira linha:

```ts
function removerDaSala(ws: WebSocket) {
  socketMatchId.delete(ws);
  const salaNum = socketRoom.get(ws);
  if (salaNum === undefined) return;
  socketRoom.delete(ws);
  const set = rooms.get(salaNum);
  if (set) {
    set.delete(ws);
    if (set.size === 0) rooms.delete(salaNum);
  }
}
```

- [ ] **Step 3: Extrair `assinarSala` e registrar o uuid**

Substituir a função `tratarMensagem` (linhas 154-194) por:

```ts
async function tratarMensagem(ws: WebSocket, raw: RawData) {
  let msg: any;
  try {
    msg = JSON.parse(raw.toString());
  } catch {
    enviar(ws, { type: "error", error: "mensagem_invalida" });
    return;
  }
  if (msg?.type === "subscribe_match") {
    await assinarSala(ws, msg);
  } else if (msg?.type === "chat_send") {
    await enviarChat(ws, msg);
  }
}

async function assinarSala(ws: WebSocket, msg: any) {
  const ref = String(msg.matchId ?? "").trim();
  if (!/^[0-9]+$/.test(ref) && !uuidRegex.test(ref)) {
    enviar(ws, { type: "error", error: "match_id_invalido" });
    return;
  }

  try {
    const match = await buscarMatch(ref);
    if (!match) {
      enviar(ws, { type: "error", error: "sala_nao_encontrada" });
      return;
    }
    const userId = socketUser.get(ws);
    if (!userId || !(await temPermissao(userId, match.id))) {
      enviar(ws, { type: "error", error: "sem_permissao" });
      return;
    }
    removerDaSala(ws);
    socketRoom.set(ws, match.salaNum);
    socketMatchId.set(ws, match.id);
    let set = rooms.get(match.salaNum);
    if (!set) {
      set = new Set();
      rooms.set(match.salaNum, set);
    }
    set.add(ws);
    enviar(ws, { type: "subscribed", matchId: match.salaNum });
  } catch (err: any) {
    console.error(`[m7arena-realtime] Erro ao assinar sala: ${err?.message}`);
    enviar(ws, { type: "error", error: "erro_interno" });
  }
}

// ── CHAT (ADR-040) ───────────────────────────────
function permitidoNoRateLimit(userId: string): boolean {
  const agora = Date.now();
  const limiar = agora - CHAT_JANELA_MS;
  const ts = (chatTimestamps.get(userId) ?? []).filter((t) => t > limiar);
  if (ts.length >= CHAT_MAX_EM_5MIN) return false;
  if (ts.length > 0 && agora - ts[ts.length - 1] < CHAT_MIN_INTERVALO_MS) return false;
  ts.push(agora);
  chatTimestamps.set(userId, ts);
  return true;
}

/** chat_send: valida (assinado, body, ban, Riot, rate limit) → INSERT → fan-out. */
async function enviarChat(ws: WebSocket, msg: any) {
  const salaNum = socketRoom.get(ws);
  const matchId = socketMatchId.get(ws);
  if (salaNum === undefined || !matchId) {
    enviar(ws, { type: "chat_error", error: "nao_assinado" });
    return;
  }
  const body = typeof msg?.body === "string" ? msg.body.trim() : "";
  if (body.length < 1 || body.length > CHAT_BODY_MAX) {
    enviar(ws, { type: "chat_error", error: "body_invalido" });
    return;
  }
  const userId = socketUser.get(ws);
  if (!userId) {
    enviar(ws, { type: "chat_error", error: "nao_autenticado" });
    return;
  }
  if (!permitidoNoRateLimit(userId)) {
    enviar(ws, { type: "chat_error", error: "rate_limited" });
    return;
  }

  try {
    const [u] = await authPool.query(
      `SELECT id, display_name AS "displayName", avatar_url AS "avatarUrl", status, riot_id AS "riotId"
         FROM users WHERE id = $1 LIMIT 1`,
      [userId]
    );
    if (!u) {
      enviar(ws, { type: "chat_error", error: "nao_autenticado" });
      return;
    }
    if (u.status === "banida") {
      enviar(ws, { type: "chat_error", error: "conta_banida" });
      return;
    }
    if (!u.riotId) {
      enviar(ws, { type: "chat_error", error: "riot_id_necessario" });
      return;
    }

    const [row] = await authPool.query(
      `INSERT INTO sala_mensagens (match_id, user_id, body)
       VALUES ($1, $2, $3)
       RETURNING id, created_at`,
      [matchId, userId, body]
    );

    const msgObj = {
      id: Number(row.id),
      user_id: userId,
      nome: u.displayName || "Jogador",
      avatar: u.avatarUrl ?? null,
      body,
      created_at: new Date(row.created_at).toISOString(),
    };
    const payload = JSON.stringify({ type: "chat_message", matchId: salaNum, msg: msgObj });
    const room = rooms.get(salaNum);
    if (room) {
      for (const cliente of room) {
        if (cliente.readyState === WebSocket.OPEN) cliente.send(payload);
      }
    }
  } catch (err: any) {
    console.error(`[m7arena-realtime] Erro no chat: ${err?.message}`);
    enviar(ws, { type: "chat_error", error: "erro_interno" });
  }
}
```

- [ ] **Step 4: Typecheck da API**

Run (em `api/`): `npm run build`
Expected: exit 0.

- [ ] **Step 5: Commit**

```bash
git add api/src/realtime/index.ts
git commit -m "feat(realtime): chat_send no WebSocket com validação, rate limit e fan-out"
```

---

### Task 4: Cron — purge TTL de 5 minutos

**Files:**
- Modify: `api/src/cron.ts` (imports linha 1-6; corpo do `runCron`)

**Interfaces:**
- Consumes: `salaMensagens` (Task 1). Produces: passo de purge no `runCron`.

- [ ] **Step 1: Imports**

Linha 1 (`"drizzle-orm"`): adicionar `lt`:

```ts
import { and, eq, lt, inArray, isNull, not } from "drizzle-orm";
```

Linha 3: incluir `salaMensagens`:

```ts
import { matches, matchPlayers, matchCodes, salaMensagens } from "../../db/schema/matches.js";
```

- [ ] **Step 2: Passo de purge no `runCron`**

Ao final de `runCron`, antes do bloco final (após a liberação de `linked` residual, antes do `return`/fim da função):

```ts
  // 3. Chat (ADR-040): mensagens de sala expiram em 5 min. O purge preguiçoso
  //    (no GET de histórico) já limpa na leitura; aqui é a rede de segurança
  //    para a tabela nunca acumular mesmo sem leituras.
  const corteChat = new Date(Date.now() - 5 * 60 * 1000);
  const [purgadas] = await d
    .delete(salaMensagens)
    .where(lt(salaMensagens.createdAt, corteChat))
    .returning({ id: salaMensagens.id });
  if (purgadas.length > 0) console.log(`[cron] chat: ${purgadas.length} mensagem(ns) expirada(s) purgada(s)`);
```

- [ ] **Step 3: Typecheck da API**

Run (em `api/`): `npm run build`
Expected: exit 0.

- [ ] **Step 4: Commit**

```bash
git add api/src/cron.ts
git commit -m "feat(cron): purge das mensagens de chat expiradas (>5min)"
```

---

### Task 5: Front — `useSalaRealtime` e `useSalaSimples` (socket do chat)

**Files:**
- Modify: `web/src/hooks/useSalaRealtime.ts`
- Modify: `web/src/hooks/useSalaSimples.ts`

**Interfaces:**
- Consumes: `ApiSalaChatMensagem` (Task 2). Produces:
  - `useSalaRealtime(salaId, opts)` — `opts` ganha `onChatMessage?: (msg) => void` e `onChatError?: (codigo: string) => void`; retorna `{ enviarChat: (body: string) => void }`.
  - `useSalaSimples(salaId, usuarioAtual, opts?: { onChatMessage?, onChatError?, onReconnect? })` — repassa ao socket e retorna `{ enviarChat }`.

- [ ] **Step 1: `useSalaRealtime` — opções e retorno**

Modificar `web/src/hooks/useSalaRealtime.ts`:

Imports (linha 15):

```ts
import { useCallback, useEffect, useRef } from "react";
import type { ApiSalaChatMensagem } from "../lib/api";
```

Na interface `UseSalaRealtimeOptions` (linha 29), adicionar antes do `enabled`:

```ts
  /** Chat (ADR-040): recebe uma mensagem nova da sala (sem debounce). */
  onChatMessage?: (msg: ApiSalaChatMensagem) => void;
  /** Chat (ADR-040): erro de envio (body_invalido, rate_limited, ...). */
  onChatError?: (codigo: string) => void;
```

Na assinatura da função (linha 40), desestruturar os novos campos:

```ts
  const { onUpdate, onReconnect, onStatusChange, onChatMessage, onChatError, enabled = true } = options;
```

Após `onStatusChangeRef.current = onStatusChange;` (linha ~52), adicionar:

```ts
  const onChatMessageRef = useRef(onChatMessage);
  const onChatErrorRef = useRef(onChatError);
  onChatMessageRef.current = onChatMessage;
  onChatErrorRef.current = onChatError;
```

No handler `ws.onmessage` (linha 88-98), após o bloco `if (dados?.type === "match_update")`:

```ts
        if (dados?.type === "chat_message") {
          onChatMessageRef.current?.(dados.msg);
        } else if (dados?.type === "chat_error") {
          onChatErrorRef.current?.(dados.error);
        }
```

Ao final da função (após o `useEffect`), retornar `enviarChat`:

```ts
  const enviarChat = useCallback((body: string) => {
    const ws = wsRef.current;
    if (!ws || ws.readyState !== WebSocket.OPEN) return;
    ws.send(JSON.stringify({ type: "chat_send", matchId: matchIdRef.current, body }));
  }, []);

  return { enviarChat };
```

- [ ] **Step 2: `useSalaSimples` — opções e repasse**

Modificar `web/src/hooks/useSalaSimples.ts`:

Assinatura (linha 50-59), adicionar o terceiro parâmetro:

```ts
export function useSalaSimples(
    salaId: number,
    usuarioAtual: {
        id: string;
        nome: string;
        tag: string;
        elo: string;
        avatar?: string;
    },
    opts: {
        onChatMessage?: (msg: import("../lib/api").ApiSalaChatMensagem) => void;
        onChatError?: (codigo: string) => void;
        onReconnect?: () => void;
    } = {}
) {
```

Na chamada `useSalaRealtime(salaId, {...})` (linha 253-279), adicionar as opções de chat e embrulhar `onReconnect`:

```ts
    useSalaRealtime(salaId, {
        onUpdate: async () => {
            ultimoUpdateRef.current = Date.now();
            const estadoAnterior = ultimoEstadoRef.current;
            const dadosSala = await sincronizarTudo('realtime');
            if (!dadosSala) return;
            const novoEstado = dadosSala.estado;

            if (novoEstado === estadoAnterior) return;

            if (IS_DEV) console.log(`📡 [Realtime] Sala ${salaId}: ${estadoAnterior} → ${novoEstado}`);
            tickRef.current = null;

            if (estadoAnterior === 'confirmacao' && novoEstado === 'preenchendo') {
                mostrar('erro', 'Confirmação cancelada — sala reaberta');
            } else if (novoEstado === 'cancelada') {
                mostrar('erro', 'Partida cancelada');
            }
        },
        onReconnect: () => {
            if (IS_DEV) console.log(`🔌 [Realtime] Reconectado — refazendo GET da sala ${salaId}`);
            sincronizarTudo('reconexao');
            opts.onReconnect?.();
        },
        onStatusChange: (conectado) => {
            wsVivoRef.current = conectado;
        },
        onChatMessage: opts.onChatMessage,
        onChatError: opts.onChatError,
    });
```

No `return { ... }` (linha 522-529), adicionar:

```ts
        atualizar: () => sincronizarTudo('manual'),
        entrar, sair, confirmar, recusar,
        enviarChat: enviarChat,
    };
```

E declarar `enviarChat` antes do return (após `recusar`, linha ~520). Como ele vem do `useSalaRealtime`, capturar o retorno — mudar a chamada `useSalaRealtime(salaId, {...})` para:

```ts
    const { enviarChat } = useSalaRealtime(salaId, { ... });
```

O restante do objeto de opções permanece o mesmo.

- [ ] **Step 3: Typecheck do front**

Run (em `web/`): `npm run lint`
Expected: exit 0.

- [ ] **Step 4: Commit**

```bash
git add web/src/hooks/useSalaRealtime.ts web/src/hooks/useSalaSimples.ts
git commit -m "feat(web): socket do chat (enviarChat + onChatMessage/onChatError) nas hooks de sala"
```

---

### Task 6: Front — hook `useSalaChat` + tradução de erros de chat

**Files:**
- Create: `web/src/hooks/useSalaChat.ts`
- Modify: `web/src/api/salamod1.ts` (mapa `ERROS_SALA`, linha 92)

**Interfaces:**
- Consumes: `api.matches.mensagens` (Task 2), `ApiSalaChatMensagem` (Task 2).
- Produces: `useSalaChat(salaId)` → `{ mensagens, naoLidas, receber, carregarHistorico, marcarLidas }`.
  - `receber(msg: ApiSalaChatMensagem)` — anexa (dedup por `id`) e incrementa `naoLidas`.
  - `carregarHistorico()` — busca `api.matches.mensagens(salaId)` e substitui a lista (limpando timers de expiração antigos).
  - `marcarLidas()` — zera `naoLidas`.
  - `mensagens` — auto-prune de >5min a cada 5s (intervalo leve).

- [ ] **Step 1: Códigos de erro de chat em `ERROS_SALA`**

Em `web/src/api/salamod1.ts`, dentro de `ERROS_SALA` (após `sala_casual`, linha 116), adicionar:

```ts
    // ── Chat de sala (ADR-040) ──
    body_invalido: 'A mensagem precisa ter entre 1 e 200 caracteres.',
    rate_limited: 'Você está enviando mensagens rápido demais. Aguarde um instante.',
    nao_assinado: 'Você ainda não está conectado a esta sala.',
    riot_id_necessario: 'Vincule seu Riot ID para usar o chat da sala.',
    erro_interno: 'Não foi possível enviar a mensagem. Tente novamente.',
```

- [ ] **Step 2: Criar `useSalaChat.ts`**

Criar `web/src/hooks/useSalaChat.ts`:

```ts
// src/hooks/useSalaChat.ts
// Estado do chat da sala (ADR-040): lista de mensagens, não lidas, histórico e
// autoexclusão de 5 min. O transporte é o socket do useSalaRealtime (já
// assinado); aqui só a UI/estado. Receber = onChatMessage; carregar = onReconnect.

import { useState, useCallback, useEffect } from 'react';
import { api, type ApiSalaChatMensagem } from '../lib/api';

const TTL_MS = 5 * 60 * 1000;      // autoexclusão
const PRUNE_INTERVALO_MS = 5000;   // varredura leve de expiração

export function useSalaChat(salaId: number) {
    const [mensagens, setMensagens] = useState<ApiSalaChatMensagem[]>([]);
    const [naoLidas, setNaoLidas] = useState(0);

    const receber = useCallback((msg: ApiSalaChatMensagem) => {
        setMensagens((prev) => {
            if (prev.some((m) => m.id === msg.id)) return prev;
            return [...prev, msg];
        });
        setNaoLidas((n) => n + 1);
    }, []);

    const marcarLidas = useCallback(() => setNaoLidas(0), []);

    const carregarHistorico = useCallback(async () => {
        try {
            const rows = await api.matches.mensagens(salaId);
            setMensagens(rows);
        } catch {
            // sem permissão ou sala sem chat — mantém vazio (a UI não renderiza)
        }
    }, [salaId]);

    // Autoexclusão de 5 min: varredura leve, remove o que expirou.
    useEffect(() => {
        const id = setInterval(() => {
            setMensagens((prev) => {
                const corte = Date.now() - TTL_MS;
                const filtrado = prev.filter((m) => new Date(m.created_at).getTime() > corte);
                return filtrado.length === prev.length ? prev : filtrado;
            });
        }, PRUNE_INTERVALO_MS);
        return () => clearInterval(id);
    }, []);

    return { mensagens, naoLidas, receber, carregarHistorico, marcarLidas };
}
```

- [ ] **Step 3: Typecheck do front**

Run (em `web/`): `npm run lint`
Expected: exit 0.

- [ ] **Step 4: Commit**

```bash
git add web/src/hooks/useSalaChat.ts web/src/api/salamod1.ts
git commit -m "feat(web): hook useSalaChat com histórico e autoexclusão de 5min"
```

---

### Task 7: Front — componente `ChatDaSala` (widget canto inferior direito)

**Files:**
- Create: `web/src/components/partidas/ChatDaSala.tsx`

**Interfaces:**
- Consumes: `enviarChat` (Task 5), `traduzirErroSala` (salamod1.ts, já existe), `onMarcarLidas`, `mensagens`, `naoLidas` (Task 6).
- Produces: componente renderizado em `SalaMod1` (Task 8). Props:

```ts
interface ChatDaSalaProps {
  mensagens: ApiSalaChatMensagem[];
  naoLidas: number;
  onMarcarLidas: () => void;
  enviarChat: (body: string) => void;
}
```

- [ ] **Step 1: Criar `ChatDaSala.tsx`**

Criar `web/src/components/partidas/ChatDaSala.tsx`:

```tsx
// src/components/partidas/ChatDaSala.tsx
// Chat da sala (ADR-040): widget flutuante no canto inferior direito, quase
// encostado na borda. Visual no padrão da sala (glass escuro + dourado #FFB700,
// cantos cortados). Só é montado em salas ativas (SalaMod1 decide).

import { useEffect, useRef, useState } from 'react';
import { MessageSquare, X, Send } from 'lucide-react';
import type { ApiSalaChatMensagem } from '../../lib/api';

const CUT_BUTTON = 'polygon(8px 0, 100% 0, 100% calc(100% - 8px), calc(100% - 8px) 100%, 0 100%, 0 8px)';

interface ChatDaSalaProps {
  mensagens: ApiSalaChatMensagem[];
  naoLidas: number;
  onMarcarLidas: () => void;
  enviarChat: (body: string) => void;
}

export default function ChatDaSala({ mensagens, naoLidas, onMarcarLidas, enviarChat }: ChatDaSalaProps) {
    const [aberto, setAberto] = useState(false);
    const [texto, setTexto] = useState('');
    const [roladoPraCima, setRoladoPraCima] = useState(false);
    const listaRef = useRef<HTMLDivElement | null>(null);

    // Auto-scroll: se não rolou pra cima, acompanha a última mensagem.
    useEffect(() => {
        const el = listaRef.current;
        if (!el || roladoPraCima) return;
        el.scrollTop = el.scrollHeight;
    }, [mensagens, roladoPraCima]);

    // Abrir o painel marca as não lidas como lidas.
    useEffect(() => {
        if (aberto) {
            onMarcarLidas();
            setRoladoPraCima(false);
        }
    }, [aberto, onMarcarLidas]);

    const handleScroll = () => {
        const el = listaRef.current;
        if (!el) return;
        const noFundo = el.scrollHeight - el.scrollTop - el.clientHeight < 24;
        setRoladoPraCima(!noFundo);
        if (noFundo && naoLidas > 0) onMarcarLidas();
    };

    const enviar = () => {
        const body = texto.trim();
        if (!body) return;
        enviarChat(body);
        setTexto('');
    };

    const minimizado = (
        <button
            onClick={() => setAberto(true)}
            className="relative p-[1.5px] bg-black hover:bg-black transition-colors"
            style={{ clipPath: CUT_BUTTON }}
            aria-label="Abrir chat da sala"
        >
            <span className="flex items-center gap-2 bg-[#0A0A0A] px-4 py-3 text-[#FFB700]"
                style={{ clipPath: CUT_BUTTON }}>
                <MessageSquare className="w-5 h-5" />
                <span className="text-xs font-black uppercase tracking-widest">Chat</span>
            </span>
            {naoLidas > 0 && (
                <span className="absolute -top-2 -right-2 min-w-[20px] h-5 px-1 rounded-full bg-red-500 text-white text-[11px] font-black flex items-center justify-center">
                    {naoLidas > 99 ? '99+' : naoLidas}
                </span>
            )}
        </button>
    );

    const expandido = (
        <div className="w-[calc(100vw-32px)] max-w-[320px] h-[380px] flex flex-col bg-black/80 backdrop-blur-md border border-[#FFB700]/25 overflow-hidden"
            style={{ clipPath: CUT_BUTTON }}>
            <div className="flex items-center justify-between px-4 py-3 border-b border-white/10 bg-black/60">
                <div className="flex items-center gap-2">
                    <MessageSquare className="w-4 h-4 text-[#FFB700]" />
                    <span className="text-xs font-black uppercase tracking-[0.2em] text-white">Chat da Sala</span>
                </div>
                <button onClick={() => setAberto(false)} className="text-white/40 hover:text-white" aria-label="Minimizar chat">
                    <X className="w-4 h-4" />
                </button>
            </div>

            <div ref={listaRef} onScroll={handleScroll}
                className="flex-1 overflow-y-auto px-3 py-3 space-y-2 scrollbar-thin">
                {mensagens.length === 0 && (
                    <p className="text-xs text-white/40 text-center pt-6">
                        Nenhuma mensagem ainda. As mensagens somem após 5 minutos.
                    </p>
                )}
                {mensagens.map((m) => (
                    <div key={m.id} className="space-y-0.5">
                        <p className="text-[11px] font-black uppercase tracking-wider text-[#FFB700]/80">{m.nome}</p>
                        <p className="text-sm text-white/90 break-words leading-snug">{m.body}</p>
                    </div>
                ))}
            </div>

            <div className="p-2 border-t border-white/10 flex items-center gap-2">
                <input
                    value={texto}
                    onChange={(e) => setTexto(e.target.value.slice(0, 200))}
                    onKeyDown={(e) => { if (e.key === 'Enter') enviar(); }}
                    maxLength={200}
                    placeholder="Digite sua mensagem..."
                    className="flex-1 bg-white/5 border border-white/10 px-3 py-2 text-sm text-white placeholder-white/30 outline-none focus:border-[#FFB700]/50"
                />
                <button onClick={enviar}
                    className="p-[1.5px] bg-[#FFB700] hover:bg-yellow-400 transition-colors shrink-0"
                    style={{ clipPath: CUT_BUTTON }}
                    aria-label="Enviar mensagem">
                    <span className="flex items-center justify-center bg-[#FFB700] px-3 py-2 text-black"
                        style={{ clipPath: CUT_BUTTON }}>
                        <Send className="w-4 h-4" />
                    </span>
                </button>
            </div>
        </div>
    );

    return (
        <div className="fixed bottom-4 right-4 z-[120]">
            {aberto ? expandido : minimizado}
        </div>
    );
}
```

- [ ] **Step 2: Typecheck do front**

Run (em `web/`): `npm run lint`
Expected: exit 0. Se `scrollbar-thin` não existir no Tailwind, remover essa classe do `div` da lista (sem impacto no visual funcional).

- [ ] **Step 3: Commit**

```bash
git add web/src/components/partidas/ChatDaSala.tsx
git commit -m "feat(web): componente ChatDaSala flutuante no canto inferior direito"
```

---

### Task 8: Front — integração no `SalaMod1.tsx` + build + deploy

**Files:**
- Modify: `web/src/pages/SalaMod1.tsx` (imports linha 5-15; hooks após `useSalaSimples` linha 98; render antes do fechamento do container, linha ~1166)

**Interfaces:**
- Consumes: `useSalaChat` (Task 6), `ChatDaSala` (Task 7), `enviarChat`/`onChatMessage`/`onChatError`/`onReconnect` (Task 5), `user?.roles` (`useAuth`).
- Produces: chat funcional na tela da sala (estados ativos).

- [ ] **Step 1: Imports**

Em `web/src/pages/SalaMod1.tsx`, na seção de imports (após linha 15), adicionar:

```tsx
import ChatDaSala from '../components/partidas/ChatDaSala';
import { useSalaChat } from '../hooks/useSalaChat';
import { traduzirErroSala } from '../api/salamod1';
import toast from 'react-hot-toast';
```

(Se `toast` já estiver importado, não duplicar.)

- [ ] **Step 2: Hooks do chat**

Após a chamada `useSalaSimples(salaId, usuarioAtual)` (linha 98) — ANTES, para poder passar os callbacks:

```tsx
    const {
        sala, jogadores, loading, erro,
        timer, codigoPartida,
        mostrarMensagem,
        erroElegibilidade, fecharErroElegibilidade, aceitarTermos, mostrarSaldoFaltante,
        atualizar,
        entrar, sair, confirmar, recusar,
        enviarChat,
    } = useSalaSimples(salaId, usuarioAtual, {
        onChatMessage: receberChat,
        onChatError: (codigo) => toast.error(traduzirErroSala(codigo)),
        onReconnect: carregarHistoricoChat,
    });
```

E logo antes dessa chamada, criar a instância do chat (as funções `receberChat`/`carregarHistoricoChat` precisam existir ANTES da chamada — declarar antes):

```tsx
    const { mensagens: mensagensChat, naoLidas: naoLidasChat, receber: receberChat, carregarHistorico: carregarHistoricoChat, marcarLidas: marcarLidasChat } = useSalaChat(salaId);
```

Ordem final dentro do componente:

```tsx
    const { user } = useAuth();
    const { perfil, refetch: refetchPerfil } = usePerfil();
    const { mensagens: mensagensChat, naoLidas: naoLidasChat, receber: receberChat, carregarHistorico: carregarHistoricoChat, marcarLidas: marcarLidasChat } = useSalaChat(salaId);
    const [showAvisoRiotId, setShowAvisoRiotId] = useState(false);
    // ... (estado existente)
```

A chamada `useSalaSimples` precisa ser ajustada (trocar a chamada existente na linha 91-98 pela versão com o terceiro parâmetro e `enviarChat` no retorno). Como `useSalaChat` é chamado antes, `receberChat`/`carregarHistoricoChat` já existem quando `useSalaSimples` roda.

- [ ] **Step 3: Gate de render + montar o widget**

Antes do `return` principal (onde `jogadorAtual` já é derivado — procure `jogadorAtual` na seção do ACTION FOOTER, linha ~914), definir:

```tsx
    const ESTADOS_CHAT = ['preenchendo', 'confirmacao', 'iniciando_partida', 'partida_iniciada', 'aguardando_revisao'];
    const ehStaff = ['admin', 'moderador', 'proprietario'].some((r) => user?.roles?.includes(r));
    const chatHabilitado = !!sala && ESTADOS_CHAT.includes(sala.estado) && (!!jogadorAtual || ehStaff);
```

No JSX, logo antes dos overlays `Edge Fog` (linha 1163-1166), dentro do container raiz, adicionar:

```tsx
            {chatHabilitado && (
                <ChatDaSala
                    mensagens={mensagensChat}
                    naoLidas={naoLidasChat}
                    onMarcarLidas={marcarLidasChat}
                    enviarChat={enviarChat}
                />
            )}
```

Nota: `useSalaChat` sempre roda (hooks não podem ser condicionais). O fetch do histórico acontece quando `ChatDaSala` monta? Não — `ChatDaSala` não carrega histórico. Quem carrega é o callback `onReconnect` (Task 5) + um fetch no mount. Para carregar no mount, o `ChatDaSala` deve chamar o histórico: passar `carregarHistoricoChat` como prop e chamar num `useEffect` de mount. Adicionar a prop:

```tsx
            {chatHabilitado && (
                <ChatDaSala
                    mensagens={mensagensChat}
                    naoLidas={naoLidasChat}
                    onMarcarLidas={marcarLidasChat}
                    enviarChat={enviarChat}
                    carregarHistorico={carregarHistoricoChat}
                />
            )}
```

E em `ChatDaSala` (Task 7), adicionar a prop `carregarHistorico` e um `useEffect` de mount:

```tsx
interface ChatDaSalaProps {
  mensagens: ApiSalaChatMensagem[];
  naoLidas: number;
  onMarcarLidas: () => void;
  enviarChat: (body: string) => void;
  carregarHistorico: () => void;
}
```

```tsx
    // Carrega o histórico ao montar (só é montado em sala ativa).
    useEffect(() => {
        carregarHistorico();
    }, [carregarHistorico]);
```

- [ ] **Step 4: Typecheck + build do front**

Run (em `web/`): `npm run lint`; depois `npm run build`
Expected: ambos exit 0.

- [ ] **Step 5: Verificação manual no browser (dev.m7arena.pro ou local)**

1. Abrir uma sala ativa com 2 contas logadas (Riot vinculado); enviar mensagem na conta A → aparece em tempo real na conta B (sem refresh).
2. Mensagem some da UI de ambos após 5 min.
3. Conta sem Riot vinculado → toast `riot_id_necessario` ao enviar.
4. Conta banida → toast `conta_banida`.
5. Sala `encerrada` → widget não aparece.
6. Visitante (sem entrar na vaga) → widget não aparece.
7. Enviar 2 mensagens em <1s → toast `rate_limited`.

- [ ] **Step 6: Deploy**

Seguir o fluxo padrão do projeto: commit → push → merge ff-only na VPS → `docker compose --env-file /root/m7arena/.env -f infra/docker-compose.yml build app realtime` + `up -d` → nginx rebuild para servir o front novo. Verificar `curl -I https://dev.m7arena.pro` = 200.

- [ ] **Step 7: Commit**

```bash
git add web/src/pages/SalaMod1.tsx web/src/components/partidas/ChatDaSala.tsx
git commit -m "feat(web): chat de sala integrado na tela da partida"
```

---

## Self-Review (preenchido)

- **Spec coverage:** migrations (Task 1), envio WS (Task 3), histórico + purge preguiçoso (Task 2), purge cron (Task 4), auto-remove 5min + não lidas (Task 6), widget canto inferior direito (Task 7), estados ativos / finalizadas sem chat / permissão participante+Riot (Task 3 + Task 8), ban/rate limit/body (Task 3).
- **Placeholders:** nenhum "TBD/TODO" — todo passo tem código e comando com saída esperada.
- **Type consistency:** `ApiSalaChatMensagem` idem em api.ts/realtime/useSalaRealtime/useSalaChat/ChatDaSala. `enviarChat` nome consistente (Task 5 → 6 → 7 → 8). `carregarHistorico` consistente (Task 6 → 8). Códigos de erro `chat_error` batem com `ERROS_SALA` (Task 3 → 6).

**Pendências pós-plano (dependências de dados reais):**
- `useSalaChat` sempre roda; o fetch do histórico é pelo mount do `ChatDaSala` + `onReconnect`. Sem condicional de hook.
- `user?.roles` em SalaMod1: confirmar que o `user` do `useAuth()` expõe `roles: string[]` (já é o tipo `ApiUser` em `api.ts`).
